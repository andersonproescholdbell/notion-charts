// const order = ['Probability', 'STS', 'Databases', 'Data Analysis', 'Other', 'Test Prep'];

// let temp = [];
    // for (var x of res.properties.Category.select.options) {
    //     temp.push(x.name);
    // }

    // console.log(temp);

// if our predefined order has the exact same categories as our data
    // if ([...order].sort().join('') == temp.sort().join('')) {
    //     for (var i of [...Array(temp.length).keys()]) {
    //         for (var x of res.properties.Category.select.options) {
    //             if (x.name == order[i]) {
    //                 console.log(x.color)
    //                 cats[x.name] = { color: toHex(x.color), order: Object.keys(cats).length, a: x.name };
    //                 catArr.push(cats[x.name]);
    //             }
    //         }
    //     }
    // } else {



// datasets.push(
    //     {
    //         type: 'line',
    //         data: Array(numDays).fill(totalHours/numDays),
    //         fill: false,
    //         borderColor: 'green',
    //         radius: 0,
    //         borderDash: [5,5]
    //     });